<!-- Header Component -->
<header class="header">
  <div class="container">
    <nav class="navbar">
      <!-- Logo (immagine, sostituisce il wordmark testuale) -->
      <a href="{{ '/' | url }}" class="logo logo-sospatto" aria-label="SOS Patto — Home">
        <img class="logo-sospatto-img" src="{{ '/IMAGES/logo-header.png' | url }}" width="281" height="160" alt="S.O.S. Patto">
      </a>

      <!-- Mobile Menu Toggle -->
      <button class="menu-toggle" id="menu-toggle" aria-label="Menu">
        ☰
      </button>

      <!-- Navigation Menu -->
      {% include "components/nav.liquid" %}
    </nav>
  </div>
</header>
